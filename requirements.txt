# KRONOS core runtime dependencies
# Rebuild the environment with:
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# The kronos package itself imports only numpy / pandas / scipy.
# yfinance is used lazily by kronos/data.py for the real-data path; if it is
# absent or the network is unavailable, the pipeline falls back to a synthetic
# regime-switching market and still runs end to end.

numpy>=1.26
pandas>=2.2          # requires the "ME" resample alias used in run_kronos.py
scipy>=1.11
yfinance>=0.2.40     # optional: real Yahoo data path (synthetic fallback otherwise)
